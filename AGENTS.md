<!-- 文件职责：向所有进入本仓库的 Codex 会话声明必须遵守的项目级工作规则。 -->

# Block Out Guides 仓库工作规则

> 本节更新时间：2026-08-03 01:01（Asia/Shanghai）

## 开始工作前

1. 完整遵守 `docs/implementation-plan.md`；它吸收了 v2 产品方案并记录已经采用的工程决策。
2. 读取 `docs/project-progress.md`，确认当前任务、依赖和下一步。
3. 每次只推进一个 TASK-001～TASK-025，除非用户明确要求多个任务或任务外工作。
4. 正式任务开始时，将对应状态更新为“进行中”并记录 Asia/Shanghai 绝对时间。

## 工作完成后

1. 更新 `docs/project-progress.md` 的正式任务表。
2. 任务外修复、规范或工具建设写入“任务外事项表”。
3. 在“会话时间线”末尾追加本轮时间、处理内容、结果、验证和下一步。
4. 更新被修改章节的局部更新时间以及进度文档顶部更新时间。
5. 只有注释检查、类型检查、Lint、相关测试和生产构建达到任务要求后，才能标记“已完成”。
6. 历史时间不明确时写“不详”或“具体时间未保留”，不得推测分钟。

## 架构与内容

1. `content/` 中经过校验的 JSON 是内容唯一事实源；页面不得重复维护业务数据。
2. 页面通过 Repository 查询内容，不直接读取具体文件路径。
3. `draft` 和 `archived` 不进入聚合页、搜索、Sitemap 或可索引页面。
4. `levelNumber` 不是解法的唯一事实；必须同时考虑 Variant、游戏版本、平台和初始棋盘。
5. 不批量预建空关卡，不把示例数据冒充已核验攻略。
6. 可注释的人工维护文件使用中文文件级职责注释；具名函数和关键回调说明维护边界。

## 真实文章生成与发布

> 本节更新时间：2026-08-03 01:01（Asia/Shanghai）

1. 本项目由个人同时维护多个站点，Agent 必须把每篇真实文章在同一任务内生成到可线上发布质量，不得把逐页人工审核作为默认流程或交付条件。
2. `content/` 只允许保存 `status: published` 的真实文章；不得出现 `pending-review`、`needs-review`、`not-started`、`in-progress`、`todo`、占位来源、占位正文或其他把审核债务转交给用户的字段和值。
3. `template-*` 与 `draft` 只能保存在 `templates/`。未完成研究、候选视频、权利不明媒体和不完整事实必须留在 `research/`，不得写入 `content/`。
4. 内容生成必须一次补齐正文、来源、版本、平台、Variant、棋盘/媒体权利、视频可嵌入状态、SEO、内链、更新时间和适用的结构化数据；无法诚实补齐时应失败关闭，不得猜测或伪造。
5. Playlist 或批量生成工具只可自动发布通过同一 Schema 与内容门禁的完整文章；单条失败必须阻止本批次写入，不能生成待确认队列。
6. 每次生成或修改真实内容必须运行 `npm run validate:content` 与完整质量链。门禁必须验证每篇内容都进入详情路由、所属聚合页、站内搜索和 Sitemap；生成后无法展示即视为失败。
7. 内容退化时应修复或从生产内容集中移除，不能改回待审核草稿，也不能留在 `content/` 里等待后续处理。

## 远端操作

1. 默认只保留本地修改，不执行 `git push`、创建 Pull Request 或部署。
2. 只有用户在当前会话明确要求远端同步、部署或发布时，才执行对应操作。

## 10 语言国际化硬性规则（全站强制）

> 本节更新时间：2026-08-05（Asia/Shanghai）

1. 全站支持 10 种语言：`en, zh-cn, pt-br, ru, de, es, fr, ja, ko, tr`。**任何 UI 文案或编辑内容的改动，必须同时在全部 10 种语言中落地，不得只改英文。**
2. 所有用户可见的 UI 字符串必须走消息键（`messages/<locale>.json` + `getMessages(locale)`/`interpolate`），**严禁在组件里硬编码英文文案**（含 aria-label、按钮、标题、卡片标签、FAQ 问答）。
3. 新增或重命名消息键时，`en.json` 为规范超集，其余 9 种语言必须同步补齐，且措辞需符合各语言习惯（游戏术语如「关卡 / 棋盘 / 助推器」不得机翻错译）。
4. 编辑型内容（obstacles/boosters/guides/updates）每种 slug 必须在 10 种语言都有对应 JSON 实体；缺失时 `editorial-repository` 回退到 `en` 源内容，但仍须补齐以避免回退。
5. **非源语言页不得因缺少译文而隐藏模块或留白**。除本条明列的 Tier A 例外外，所有攻略模块（Video walkthrough / Quick tips / Step-by-step / Failure points / FAQ）对所有语言一致渲染；视频本身是英文 walkthrough，故攻略正文同源英文、随视频一同展示。
6. **Tier A 例外（唯一允许的隐藏）**：棋盘模块的 `level.summary` 与 `BoardProfile` 地标正文为英文源文且无译文，非源语言页按 `showProse = sourceLocale === locale` 隐藏，**仅此两处**；新增任何语言门控前必须先提供译文，否则视为违规。
7. 每次改动后必须运行 `npm run validate:i18n`（脚本 `scripts/check-i18n-parity.mjs`）：校验 10 语言消息键完全一致、编辑内容 10 语言覆盖完整。该校验已接入 `npm run quality`，CI 不得跳过。
8. 验证多语言渲染时，至少抽测 2–3 种非英文语言（含 CJK 与西里尔字母）的关卡详情页，确认无英文残留、视频比例正常（16:9 且高度 ≤80vh）、三栏布局齐全。
